[project]
name = "api-core"
version = "0.1.0"
description = "Robyn backend for OmniVault"
dependencies = [
    "robyn",
    "edgedb"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "ruff",
    "pytest"
]
